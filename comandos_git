Primeiros comandos para subir o repositorio, e modificar ele atraves do git

git config --global user.name ""
git config --global user.email ""

git status - Para ver quais arquivos estão no controle de versões, quais commit foi feito emuito mais.
git add "nome-do-arquivo" - Adicionar arquivo no controle de versões, para depois fazer o commit 
git add . - Para Adicionar, todos os arquivos que ainda falta commitar na proxima versão.
git commit -m "nome-do-commit" - Após adicionar os arquivos modificados,  para a proxima versão, ai é só fazer o commit.
git push  --set-upstream origin master - Define qual branch será usada nos push
git push - sobe os novos commits feitos para o repositorio da nuvem.
git reflog - Mostra o historico de versões.

git remote add origin link-do-seu-repositorio.git - Adiciona o repositorio ao projeto.
git remote set-url origin link-da-url - Mudar a url do projeto.


Comandos Avançados ------------------------------------------------------------

git reset --hard numero-da-versao -  Volta a versão anterior ou a especificada pelo codigo, encontrado, no comando reflog

git branch - Mostra as branch disponiveis no seu projeto.
git branch nome-da-nova-branch - Cria uma nova branch para seu codigo.
git branch -b nova-branch branch-master - Cria uma nova branch com base na branch principal.
git checkout nome-da-branch - para mudar de branch e trabalhar com os commits e alterações


Unir branch a uma principal - passo a passo ----------------------------------

entrar na branch principal, e digitar
git pull - Para puxar todas as atualizações do repositorio online.
git merge nome-da-branch - O nome da branch é aquela que você quer unir na branch principal.
git push para subir o codigo para o repositorio.


Arquivo GIT IGNORE ------------------------------------------------------------

touch .gitignore - comando para criar o arquivo git ignore, aqui você informa todos os arquivos que não serão adicionado ao seu projeto



-------------------------------------------------

git branch -m master main
git fetch origin
git branch -u origin/main main
git remote set-head origin -a
